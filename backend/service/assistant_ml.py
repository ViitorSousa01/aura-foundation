def generate_response(prompt: str) -> dict:
    # Simulação de uma resposta gerada pela IA
    response = {
        "response": f"Simulated response to '{prompt}'",
        "confidence": 0.95
    }
    return response
